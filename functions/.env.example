# ---------------------------------------------------------------
# Copy to .env and fill in real values. NEVER commit the real .env.
#
# Non-secret config only — CCB_API_USERNAME and CCB_API_PASSWORD are
# set separately as Firebase secrets, not here:
#   firebase functions:secrets:set CCB_API_USERNAME
#   firebase functions:secrets:set CCB_API_PASSWORD
# ---------------------------------------------------------------

CCB_API_URL=https://gacny.ccbchurch.com/api.php
